[pytest]
addopts = --import-mode=importlib
asyncio_mode = strict
asyncio_default_fixture_loop_scope = class
pythonpath = .
markers =
    integration: tests that exercise real external processes or services
