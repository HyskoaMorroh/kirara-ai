# 3.3.0a7 交接记录（2026-08-18）

> 上一轮 Codex 会话因上游 `content-blocked` 卡死，无法继续。本文件记录截至 2026-08-18 02:45
> 的**已验证事实**，供新会话接续。所有结论均由命令输出或文件存在性证实，未经验证的推断已标注。

## 现场

- 工作树：`E:\output\kirara-ai\kirara-ai3.3.0a5`（项目 B，git 仓库）
- 分支：`feat/workflow-canvas-and-theme-overhaul`（**不是 main**）
- HEAD：`8e85528 wip: 事务持久化、空间索引、请求代际与测试补齐`
- 本地领先远程 8 个提交，**均未推送**
- 对照基线：`E:\output\kirara-ai\kirara-ai3.3.0a4`（项目 A，无 git）
- 计划文档：`docs/superpowers/plans/2026-08-17-kirara-ai-excellence-overhaul.md`（477 行，73 步）

## 计划完成度（已回填到计划文档的勾选框）

| Task | 完成 / 待办 | 状态 |
|---|---|---|
| 1 版本与发布契约 | 5 / 2 | 版本三处已统一；CI 断言与提交信息未对齐 |
| 2 可恢复持久化 | 10 / 0 | 完成 |
| 3 模型目录 CAS | 6 / 0 | 完成 |
| 4 前端请求代际 | 8 / 0 | 完成 |
| 5 增量布局与有界历史 | 8 / 0 | 完成 |
| 6 语义主题与无障碍 | 0 / 8 | **未开始** |
| 7 首次就绪与扩展契约 | 0 / 9 | **未开始** |
| 8 文档 / CHANGELOG / Docker | 0 / 7 | **未开始** |
| 9 完全替代证明 | 2 / 8 | 仅全量测试已跑 |

合计 39 完成 / 34 待办。

## 已验证通过的测试

```
后端：.venv-win/Scripts/python.exe -m pytest ./tests -q
      2 failed, 447 passed, 1 skipped   （51 秒）

前端：npm --prefix webui run test:unit -- --run
      14 files, 58 passed               （2.3 秒）

类型：npm --prefix webui run type-check
      vue-tsc --noEmit 无输出 = 通过
```

## 两个后端失败（已定位根因，均属 Task 1 收尾）

**1. `tests/test_webui_build_contract.py::test_bundled_webui_lockfile_uses_an_available_registry`**

`webui/yarn.lock` 混入了 `registry.npmmirror.com` 源地址，测试要求只允许
`registry.npmjs.org` / `github.com` / `codeload.github.com`。命中三条：

```
@babel/helper-string-parser-7.29.7
@babel/helper-validator-identifier-7.29.7
@babel/parser-7.29.8
```

修法：把这三条 `resolved` 换回 npmjs.org，或用官方源重新生成 yarn.lock。

**2. `tests/test_release_workflow_contract.py::test_release_preflight_checks_contracts_and_the_versioned_webui_build`**

测试断言 workflow 中存在单行命令：

```
python -m pytest tests/test_release_workflow_contract.py tests/test_webui_build_contract.py -q
```

而 `.github/workflows/release-preflight.yml:73` 改成了多行续行形式（并新增了
`tests/test_release_artifact_contract.py`）。功能上是增强，但断言未同步更新。

修法：更新测试断言以匹配新的多行形式，**不要**把 workflow 改回单行（那会丢掉新增的
artifact contract 检查）。

## 已确认落地的实现

```
Task 1  pyproject.toml 3.3.0a7 / uv.lock 3.3.0a7 / webui/package.json 3.3.0-a7
        tests/test_release_artifact_contract.py:80  assert_distribution_contents()

Task 2  kirara_ai/workflow/persistence.py
          :19  class FileMutation      :39  class FileTransaction
          :60  commit()                :122 recover_directory()
          :347 atomic_write_text()
        registry.py:91  snapshot_builders()
        web/api/workflow/routes.py:50  已改用 snapshot_builders()

Task 3  scheduler/model_catalog.py:55  backend_config_fingerprint()
        scheduler/scheduler.py:136,167  取指纹 + CAS 复核

Task 4  webui/src/composables/useLatestRequest.ts  begin/isCurrent/cancel
        WorkflowEditor.vue 的 finally 已改为条件清理（不再裸 return）

Task 5  webui/src/components/workflow/spatial-index.ts:22  GridSpatialIndex
        useLayout.ts:436  layoutMissingNodes()
```

## 环境注意事项

- **`.venv` 已损坏，不要删也不要修。** 它是残留的 Linux 风格环境，只剩
  `lib64 -> lib` 符号链接和 `pyvenv.cfg`，没有 Windows 可执行 Python。`uv` 尝试重建时
  因无权删除该符号链接而在测试启动前退出。
- 改用 `.venv-win/Scripts/python.exe`（已建好，依赖齐全，上述测试即用它跑的），
  或 `uv run --isolated --frozen --python 3.11`。
- `.venv-win` 里没装 `uv` 模块，`python -m uv lock --check` 会失败；需要时用全局 `uv`。
- 前端测试脚本名是 **`test:unit`**，不是 `test`。计划文档里写的
  `npm --prefix webui test` 会报 `Missing script: "test"`。
- `docs/LOGO.jpg` 是用户私有文件，**未纳入版本控制**（提交时以
  `':!docs/LOGO.jpg'` 显式排除）。保持未跟踪、不改动、不打包。

## 未验证 / 待确认

- `webui/yarn.lock` 中 `@vue/compiler-dom` 被提升到 3.5.41，而 `vue` 及其余编译器包仍是
  3.5.13。运行时目前不报错，但跨环境可复现性存疑。计划外发现，需按原版本族锁齐后重跑
  冻结安装验证。
- Task 6 的 `webui/src/theme/palettes.ts` 在提交 `54cb5c3` 中被改过，但当前**不含任何
  语义 token**（grep `focus|overlay|selection|muted` 命中 0 次），且
  `webui/tests/workflow-accessibility.test.ts` 不存在。故 Task 6 判定为未开始。
- Docker 构建从未在本机成功完成（此前因本地代理干扰 apt-get 失败）。Task 9 Step 6 属
  未验证项，按计划要求应作为“未解决的发布风险”上报，不得静默接受。
- 上一轮启动的三个子代理（Dalton / Kepler / Noether）审计报告未回收，已随会话丢失。

## 新会话建议开场

```
接续 kirara-ai 3.3.0a7 改造。工作树 E:\output\kirara-ai\kirara-ai3.3.0a5，
分支 feat/workflow-canvas-and-theme-overhaul，HEAD 8e85528。

先读 docs/superpowers/plans/2026-08-17-kirara-ai-excellence-overhaul.md 的勾选状态
和 docs/superpowers/plans/2026-08-18-handoff.md，不要重做已完成的 39 步。

优先级：先修 Task 1 剩余 2 步（yarn.lock 源地址 + preflight 断言），让后端
447 passed 变成全绿；再按 Task 6 → 7 → 8 → 9 顺序推进。

环境：用 .venv-win/Scripts/python.exe 或 uv --isolated --frozen --python 3.11；
不要触碰损坏的 .venv。前端测试脚本是 test:unit。
```
